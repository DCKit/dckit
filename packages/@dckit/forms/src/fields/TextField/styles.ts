import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  helperText: {
    position: 'absolute',
    top: 50,
    lineHeight: '1.2em',
    cursor: 'default',
  },
}))

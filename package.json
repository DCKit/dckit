{
  "name": "dckit",
  "private": true,
  "scripts": {
    "build": "FORCE_COLOR=true lerna run build --stream",
    "postinstall": "lerna bootstrap --hoist --ci"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
